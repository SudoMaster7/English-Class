import express from 'express';
const router = express.Router();

// Placeholder - Coming in Phase 2

router.get('/', (req, res) => {
    res.json({
        success: true,
        message: 'Games endpoints - Coming soon in Phase 2'
    });
});

export default router;
